# Preview refit request
Prepare this AI-generated implementation for code review. You may edit code and tests.
Contract: render(name) returns "Hello, " + name. Input must be a string; otherwise raise TypeError.
Local convention: direct functions; no interchangeable providers currently exist.
The existing test passes, but has incomplete coverage. Review the name boundary too.
Audience: code reviewer. scope=preview.py; granularity=細部; strength=テスト実行.
Use only this fixture. Run python3 -m unittest discover -v.
