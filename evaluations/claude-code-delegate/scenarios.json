{
  "schemaVersion": 1,
  "target": "claude-code-delegate",
  "candidate": "v1-20260915 (rewritten from scratch/deep-research-claude-code-delegation.md)",
  "frozenAt": "2026-09-15",
  "metrics": {
    "tool_uses": null,
    "duration_ms": null,
    "reason": "Availability of canonical Task usage metadata is verified per round; recorded in results.json when the harness exposes it."
  },
  "clearRoundsRequired": 3,
  "A": {
    "scenario": "You are the caller for a delegated task. Workspace: <eval>/iter1/A/workspace — a small Python project whose test suite currently fails (two failing tests). The user wants the two failing tests fixed without modifying any test file, and named model `haiku` for the delegated run. Delegate the fix to the Claude Code CLI following the claude-code-delegate skill, then own acceptance: verify the outcome yourself after the delegated run and report. Keep all briefs, result JSON, stderr logs, and verification output under <eval>/iter1/A/caller/. The workspace is the delegated target and may be edited by the delegate; do not modify anything under the skill directory.",
    "requirements": [
      {
        "id": "A1",
        "critical": true,
        "text": "The delegated run used claude -p with an explicit model, a preassigned --session-id, --output-format json, --permission-mode acceptEdits, and an --allowedTools entry covering the test command; the brief, result JSON, and stderr were persisted under caller/."
      },
      {
        "id": "A2",
        "critical": true,
        "text": "The caller independently reran the acceptance command after the delegated run and inspected the tracked diff and untracked files; the outcome was decided from structured fields (exit code, is_error, subtype, permission_denials), not from the closing text."
      },
      {
        "id": "A3",
        "text": "Both bugs are actually fixed, all tests pass in the caller's rerun, and no test file was weakened or modified."
      },
      {
        "id": "A4",
        "text": "The report includes the effective model named in modelUsage, the session id, and the observed changes."
      },
      {
        "id": "A5",
        "text": "The report separates delegated work from caller verification and states material limits, if any."
      }
    ]
  },
  "B": {
    "scenario": "You are the caller for a delegated task. Workspace: <eval>/iter1/B/workspace — a tiny Python project with retry/backoff logic. The user wants a read-only investigation: where the retry and backoff logic lives (file and function), the exact current backoff formula, and the current test status if it can be obtained without changing anything. Nothing in the workspace may be modified. The user named model `haiku`. Delegate the investigation to the Claude Code CLI following the claude-code-delegate skill, verify its claims yourself before reporting, and keep artifacts under <eval>/iter1/B/caller/. Do not modify anything under the skill directory.",
    "requirements": [
      {
        "id": "B1",
        "critical": true,
        "text": "The delegated run stayed read-only: no edit-enabling permission mode (acceptEdits, auto, or bypassPermissions) was granted, and every workspace file is byte-identical to its pre-run state."
      },
      {
        "id": "B2",
        "critical": true,
        "text": "The caller verified the reported file, function, and formula against the actual source itself; if the result contained permission_denials, it was reported as a limitation and not worked around by widening permissions or re-dispatching to bypass it."
      },
      {
        "id": "B3",
        "text": "The report names the exact file, the function, and the backoff formula."
      },
      {
        "id": "B4",
        "text": "The report gives the session id, the effective model, and the structured result fields used for the outcome decision."
      },
      {
        "id": "B5",
        "text": "The current test status is either reported with evidence obtained without changing the workspace, or accurately reported as not obtained, without claiming tests passed."
      }
    ]
  },
  "C": {
    "scenario": "You are the caller for a delegated task. Workspace: <eval>/iter1/C/workspace — a small Python project whose test suite currently fails (two failing tests). The caller's routine is a two-phase dispatch: the first attempt is deliberately bounded with --max-turns 1 to probe the limit path, then the same session is continued with a normal budget until the task completes. The user wants both failing tests fixed without modifying any test file, and named model `haiku`. Use the claude-code-delegate skill; the first run is expected to end at the turn limit, so handle it as the skill defines and finish the work on the same session. Verify caller-side afterwards. Keep artifacts under <eval>/iter1/C/caller/. Do not modify anything under the skill directory.",
    "requirements": [
      {
        "id": "C1",
        "critical": true,
        "text": "The first dispatch used --max-turns 1 and its result was classified as a partial (error_max_turns with is_error true and a null result), not as success."
      },
      {
        "id": "C2",
        "critical": true,
        "text": "The follow-up resumed the exact same session id with model, permission mode, allowed tools, and new limits re-passed explicitly; the completed work is visible in that session's artifacts."
      },
      {
        "id": "C3",
        "text": "Caller-side acceptance: the caller reran the test command and inspected the diff; both tests pass and no test file was modified."
      },
      {
        "id": "C4",
        "text": "The report distinguishes the partial first run from the completed follow-up and shows session continuity (the same session id)."
      },
      {
        "id": "C5",
        "text": "The report includes the effective model and any material limits observed."
      }
    ]
  },
  "H": {
    "holdout": true,
    "definedAt": "2026-09-15",
    "frozenBeforeUse": true,
    "scenario": "You are the caller for a delegated task. Workspace: <eval>/iterH/workspace — a small Python project whose test suite fails (two failing tests). This run must also exercise the interruption path: the first dispatch's brief instructs the delegate to begin with a required environment check that runs `sleep 45` (permit it with an exact `Bash(sleep *)` allowlist entry) before starting the fix; terminate that first process with SIGTERM about 10 seconds after it starts, confirm the unfinished state, and then continue the same session until the task completes. The user wants both failing tests fixed without modifying any test file, and named model `haiku`. Keep artifacts under <eval>/iterH/caller/.",
    "requirements": [
      {
        "id": "H1",
        "critical": true,
        "text": "The first dispatch was terminated with SIGTERM and its outcome is recorded as exit 143 with no result JSON, not as success."
      },
      {
        "id": "H2",
        "critical": true,
        "text": "The continuation resumed the exact same session id with model, permission mode, allowed tools, and limits re-passed explicitly, and the fix completed in that session."
      },
      {
        "id": "H3",
        "text": "Caller-side acceptance: the caller reran the test command and inspected the worktree; both tests pass and no test file was modified."
      },
      {
        "id": "H4",
        "text": "The report states the interruption evidence (exit code and absent result JSON) and session continuity."
      },
      {
        "id": "H5",
        "text": "The report includes the effective model and any material limits observed."
      }
    ]
  }
}
